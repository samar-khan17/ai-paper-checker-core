\# Build \& Distribution Notes — Hussam



Verified the build/distribution setup for Smart Paper Checker:

\- installer.iss (Inno Setup script)

\- SmartPaperChecker.spec (PyInstaller spec)

\- RUN\_APP.bat (one-click launcher using bundled Python 3.14 venv)



Tested locally on Windows. Build files confirmed working as part of the

distribution pipeline.

